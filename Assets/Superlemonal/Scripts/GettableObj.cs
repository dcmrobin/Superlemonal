using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GettableObj : MonoBehaviour
{
    public enum rotation{Fixed, Free};
    public rotation rotationType = rotation.Free;
}
